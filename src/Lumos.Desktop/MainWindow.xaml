<Window x:Class="Lumos.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Lumos"
        Icon="lumos.ico"
        Width="1100" Height="720"
        MinWidth="720" MinHeight="480"
        Background="{StaticResource BgDeepBrush}"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <!-- Custom window chrome: keep the resize + maximize behavior but draw
         our own title bar with themed buttons. -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ============ TITLE BAR ============ -->
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="1"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Background="{StaticResource BgDeepBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Title (also draggable area) -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0"
                                VerticalAlignment="Center">
                        <Ellipse Width="6" Height="6" Fill="{StaticResource AccentGoldBrush}"
                                 VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="LUMOS"
                                   Foreground="{StaticResource AccentGoldBrush}"
                                   FontFamily="{StaticResource DisplayFontFamily}"
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="// secure"
                                   Style="{StaticResource MonoLabel}"
                                   FontSize="10"
                                   Margin="12,0,0,0"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Chrome buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal"
                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="MinimizeBtn"
                                Style="{StaticResource WindowChromeButton}"
                                Content="&#xE921;"
                                ToolTip="Minimize"
                                Click="Minimize_Click"/>
                        <Button x:Name="MaximizeBtn"
                                Style="{StaticResource WindowChromeButton}"
                                Content="&#xE922;"
                                ToolTip="Maximize"
                                Click="MaximizeRestore_Click"/>
                        <Button x:Name="CloseBtn"
                                Style="{StaticResource WindowChromeCloseButton}"
                                Content="&#xE8BB;"
                                ToolTip="Close"
                                Click="Close_Click"/>
                    </StackPanel>
                </Grid>

                <!-- Thin grey separator below the title bar -->
                <Rectangle Grid.Row="1"
                           Height="1"
                           Fill="{StaticResource BorderBrush}"/>
            </Grid>

            <!-- ============ ROUTED CONTENT ============ -->
            <ContentControl x:Name="MainContent" Grid.Row="1"/>
        </Grid>
    </Border>
</Window>
